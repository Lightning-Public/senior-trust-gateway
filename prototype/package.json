{
  "name": "senior-trust-gateway-prototype",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "test": "vitest run",
    "build:kisa-snapshot": "node scripts/build-kisa-snapshot.mjs",
    "check:snapshot-script": "node --check scripts/build-kisa-snapshot.mjs"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "vite": "^7.1.3",
    "vitest": "^3.2.4"
  }
}
